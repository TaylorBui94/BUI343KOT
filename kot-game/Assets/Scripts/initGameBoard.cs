using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class initGameBoard : MonoBehaviour
{
    // Start is called before the first frame update
    public void initPlayers() // initialize all players, resources, cards, die, etc
    {
        int playercount = numPlayers.playercount;
        Debug.Log("inside Start() of initGameboard");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
